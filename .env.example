# Backend API URL (optional - defaults to production)
# VITE_BACKEND_URL=http://localhost:3000

# For local development, create a .env file and uncomment the line above
# The extension will use the production backend by default
